meta {
  name: Create Event
  type: http
  seq: 2
}

post {
  url: {{base_url}}/events
  body: json
  auth: inherit
}

body:json {
  "Visited friends at 2 pm"
}

settings {
  encodeUrl: true
  timeout: 0
}
