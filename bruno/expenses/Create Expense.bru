meta {
  name: Create Expense
  type: http
  seq: 2
}

post {
  url: {{base_url}}/expenses
  body: json
  auth: inherit
}

body:json {
  "Bought a bottle of wine"
}

settings {
  encodeUrl: true
  timeout: 0
}
